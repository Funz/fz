# Python Interpreter Test File - With Context
# This file tests context lines (function definitions)

#@import math
#@def cylinder_volume(r, h):
#@    return math.pi * r ** 2 * h
#@
#@def cylinder_surface_area(r, h):
#@    return 2 * math.pi * r * (r + h)

Cylinder Parameters:
- Radius: $radius
- Height: $height

Calculations using functions:
- Volume: @{cylinder_volume($radius, $height)}
- Surface Area: @{cylinder_surface_area($radius, $height)}
- Volume/Area Ratio: @{cylinder_volume($radius, $height) / cylinder_surface_area($radius, $height)}
