# contrib/

Staging area for fz wrappers whose own `Funz/fz-<code>` repository does not exist yet.
Each subdirectory is a complete wrapper repository (layout of
`skills/fz/code-wrapper.md`), to be moved to its own repository and removed from here.

- `fz-Simulate/`: Studsvik SIMULATE and the CASMO5 -> CMS-LINK -> SIMULATE chain
  (target: `Funz/fz-Simulate`).
